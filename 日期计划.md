# 日期计划

> 2026-05-23 建立

---

## 待办

### 1. 验收 Phase 4 — 3/4 通过 + UI 重构 v2 完成

- [x] **4.1 权限系统**：三级优先级 + 默认权限 + Agent 拦截，全部就绪
- [x] **4.2 UI 升级**：Markdown 渲染 + 语法高亮 + diff 展示，`_print_assistant_bubble()` 已集成
- [x] **4.2b UI 重构 v2**（2026-05-24）：先审后执行 + 欢迎屏精简 + 状态栏增强 + 流式修复 + 权限弹窗升级 + 会话自动批准
- [x] **4.3 部分修复**：Config.max_tokens 字段已加（128000），agent.py 崩溃 bug 已修
- [ ] **修复 4.3 剩余**：ContextManager 从 Config 动态读取 + `/env max-tokens` 命令 + 摘要英文化
- [x] **4.4 流式思考**：Thinking 指示 + reasoning 流式 + 耗时统计，全部就绪
- [ ] 端到端测试：修复 4.3 后在 cmd.exe/PowerShell 中实际对话验证

### 2. 完善记忆系统（路径 B：write_file + append）✅ 已完成

- [x] **task 1**: `tools/files.py` — `write_file` 增加 `append` 参数，WRITE_FILE_TOOL 增加 append Schema
- [x] **task 2**: `prompting.py` — 重写 Memory 节（4 种类型定义 + 负面清单 + 读写规则 + 验证步骤）
- [x] **task 3**: `agent.py` — 删除 `_try_persist_auto_memory` 方法及其在 `_run_llm_loop` 中的调用
- [x] **task 4**: `memory.py` — 删除 11 个写入链方法（save_auto_memory 整条链），保留 16 个读取/注入方法
- [x] 验证：4 个文件 py_compile 通过 + 确认 11 个已删方法不存在

### 3. 创建 GitHub 仓库 ✅ 已完成

- [x] `git init` + `.gitignore`（排除 `~/.xcode/`、`__pycache__/`、`.env` 等）
- [x] 初始 commit：40 文件，5986 行
- [x] 推送至 https://github.com/acofGDUT/Xcode.git

### 4. 第一次 UI 重构 ✅ 已完成（2026-05-24）

- [x] **先审后执行**：write_file/edit_file 在 execute 前构造 diff 并展示，用户确认后才执行
- [x] **欢迎屏精简**：17 行 ASCII art → 3 行文字
- [x] **用户气泡**：Panel 包裹 → `▸ text` dim 前缀
- [x] **状态栏增强**：token 估算 + 工具计数 + 会话时长
- [x] **权限弹窗升级**：`input()` → `radiolist_dialog`（yes/yes_all/no）
- [x] **会话自动批准**：`_session_auto_approve` 字典，write/shell 分 scope
- [x] **工具调用展示**：`_render_tool_call()` 青色粗体 + args 缩进
- [x] **流式代码块修复**：检测 ``` 边界，代码块内容不再重复流式输出
- [x] **Ctrl+C 处理**：工具执行期间 KeyboardInterrupt 捕获
- [x] **权限模块解耦**：permissions.py 移除 Console 依赖
- [x] **崩溃 bug 修复**：Config.max_tokens 字段缺失导致 agent.py 启动崩溃
- [x] 验收报告：PHASE4_ACCEPTANCE.md 第三次验收记录
