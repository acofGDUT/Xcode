# 日期计划

> 2026-05-23 建立

---

## 待办

### 1. 验收 Phase 4（4.1✅ 4.2❌ 4.3⚠️ 4.4✅）

- [ ] **修复 4.2**：`OutputRenderer.render()` 集成到输出链路（当前 `_print_assistant_bubble` 无调用者，Markdown 渲染白写）
- [ ] **修复 4.3**：`ContextManager.MAX_TOKENS` 改为从 Config 读取或根据 model 推断（当前硬编码 200k，小模型不触发压缩）
- [ ] **修复 4.3**：摘要提示词改为英文，与 BASE_SYSTEM_PROMPT 一致
- [ ] 端到端测试：修复后在 cmd.exe/PowerShell 中实际对话验证

### 2. 完善记忆系统（路径 B：write_file + append）✅ 已完成

- [x] **task 1**: `tools/files.py` — `write_file` 增加 `append` 参数，WRITE_FILE_TOOL 增加 append Schema
- [x] **task 2**: `prompting.py` — 重写 Memory 节（4 种类型定义 + 负面清单 + 读写规则 + 验证步骤）
- [x] **task 3**: `agent.py` — 删除 `_try_persist_auto_memory` 方法及其在 `_run_llm_loop` 中的调用
- [x] **task 4**: `memory.py` — 删除 11 个写入链方法（save_auto_memory 整条链），保留 16 个读取/注入方法
- [x] 验证：4 个文件 py_compile 通过 + 确认 11 个已删方法不存在

### 3. 创建 GitHub 仓库

- [ ] `git init` + `.gitignore`（排除 `~/.xcode/`、`__pycache__/`、`.env` 等）
- [ ] 初始 commit：当前全部代码 + 文档
- [ ] 在 GitHub 创建远程仓库
- [ ] `git remote add origin` + `git push`
